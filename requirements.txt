# =============================================================================
# Fin-Guard Analytics – Python Dependencies
# Install: pip install -r requirements.txt
# Python : >= 3.11
# =============================================================================

# ─── Core Data & Database ─────────────────────────────────────────────────────
psycopg2-binary==2.9.9          # PostgreSQL adapter (binary = no C compiler needed)
SQLAlchemy==2.0.36              # ORM + Core SQL engine (updated for compatibility)
pandas==2.2.3                   # DataFrames for ETL, EDA, Excel export
numpy==2.1.3                    # Numerical ops (used in data generator)
tabulate==0.9.0                 # Required by pandas .to_markdown() in investigator.py

# ─── Excel Reporting ──────────────────────────────────────────────────────────
openpyxl==3.1.5                 # Read/write .xlsx (used by pandas & generate_excel_report)
XlsxWriter==3.2.0               # Advanced Excel writer with charts & formatting

# ─── AI Agent – LangChain Stack ────────────────────────────────────────────────
# NOTE: All langchain-* packages must be from the same release family (0.3.x)
langchain==0.3.13               # Core orchestration framework
langchain-core==0.3.28          # Base abstractions: messages, tools, runnables (REQUIRED EXPLICIT)
langchain-openai==0.2.14        # OpenAI chat + embeddings integration for LangChain
langchain-community==0.3.13     # Community tools (SQL agent, etc.)
openai==1.57.4                  # OpenAI Python SDK (used directly by langchain-openai)

# ─── Scheduling ───────────────────────────────────────────────────────────────
APScheduler==3.10.4             # BlockingScheduler used in agent/main.py

# ─── HTTP & API Clients ───────────────────────────────────────────────────────
httpx==0.27.2                   # Async HTTP – used in notifier.py & alert_tool.py
requests==2.32.3                # Sync HTTP – general use

# ─── Config & Secrets ─────────────────────────────────────────────────────────
python-dotenv==1.0.1            # Load .env file
pydantic==2.10.3                # Data validation (used by pydantic-settings)
pydantic-settings==2.6.1        # BaseSettings – used in agent/config.py

# ─── Retry Logic ──────────────────────────────────────────────────────────────
tenacity==9.0.0                 # @retry decorator – used in agent/investigator.py

# ─── Terminal UI ──────────────────────────────────────────────────────────────
rich==13.9.4                    # Rich terminal output – Console, Panel, Table in main.py
loguru==0.7.2                   # Structured logging – used in all agent files

# ─── Visualization (EDA Notebooks) ────────────────────────────────────────────
matplotlib==3.9.3               # Plotting for notebooks
seaborn==0.13.2                 # Statistical plots
plotly==5.24.1                  # Interactive plots

# ─── Jupyter ──────────────────────────────────────────────────────────────────
jupyter==1.1.1
ipykernel==6.29.5

# ─── Date Utilities ───────────────────────────────────────────────────────────
python-dateutil==2.9.0.post0
